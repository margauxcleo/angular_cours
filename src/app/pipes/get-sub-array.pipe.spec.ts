import { GetSubArrayPipe } from './get-sub-array.pipe';

describe('GetSubArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSubArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
