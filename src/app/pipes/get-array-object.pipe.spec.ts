import { GetArrayObjectPipe } from './get-array-object.pipe';

describe('GetArrayObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new GetArrayObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
